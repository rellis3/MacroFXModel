"""pylego.indicators — shared indicator maths for the Python side.

Category-A bricks (PYTHON_LEGO.md §3): the canonical implementation lives in
JS; the Python module is golden-tested against generated vectors so the two
readers can never drift.
"""
