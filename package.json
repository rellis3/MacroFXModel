{
  "name": "macrofxmodel-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MacroFX persistent server — dashboard + second-by-second price monitoring",
  "scripts": {
    "start": "node server.js",
    "dev":   "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
