# Chrome profile holds YOUR live CME session cookies — never commit it.
.chrome-profile/
# Recon captures raw pages/JSON that may embed session ids.
out/

# Holds live QuikStrike insid/qsid - session identifiers, never commit.
.qs_session.json
