# Copy this to .env and fill in your key: cp .env.example .env
# .env lives inside oi_recon/ (same folder as databento_oi_pull.py) and is
# already gitignored by the repo-wide ".env" pattern -- never commit the
# real file. Used by databento_oi_pull.py only.

DATABENTO_API_KEY=db-your-key-here
